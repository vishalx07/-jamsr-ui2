{
  "name": "@jamsrui/template",
  "version": "0.0.9",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "npx eslint src --ext .ts,.tsx --fix",
    "build": "tsup",
    "prepack": "clean-package",
    "postpack": "clean-package restore"
  },
  "clean-package": "./clean-package.config.json",
  "devDependencies": {
    "@types/react": "catalog:",
    "clean-package": "^2.2.0",
    "react": "catalog:"
  },
  "peerDependencies": {
    "react": "catalog:"
  },
  "dependencies": {}
}